import { Box, styled } from "@mui/system";

export const Container = styled(Box)`
  width: 100%;
  max-width: 1080px;
  min-height: 100vh;
  margin: 0 auto;
  background-color: var(--light-Color);
`;
